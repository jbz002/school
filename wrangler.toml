# Cloudflare Pages 配置文件
# 用于静态站点 (SPA) 部署

name = "battery-assembly-training-system"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# SPA 路由配置 - 所有路由重定向到 index.html
# 这对于 React Router 是必需的
[build]
command = "npm run build"
cwd = "."
